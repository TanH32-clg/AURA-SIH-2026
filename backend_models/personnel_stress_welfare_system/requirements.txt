# Core
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# Model 3: gradient boosting (falls back to sklearn HistGradientBoosting
# automatically if this isn't installed -- see models.py)
xgboost>=2.0

# Explainability (falls back to permutation-based explanations if missing --
# see risk_scoring.py)
shap>=0.45

# Backend API
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9   # required by FastAPI for UploadFile
